#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../w3cjs/bin/w3cjs" "$@"
  ret=$?
else 
  node  "$basedir/../w3cjs/bin/w3cjs" "$@"
  ret=$?
fi
exit $ret
